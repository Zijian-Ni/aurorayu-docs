# Aurora AI 文档主页

欢迎来到 Aurora AI 文档站点。本站基于 Docsify 搭建。

- 左侧导航已拆分为 **8 个独立页面**，分别介绍：角色与约束、多模型调度、Prompt 结构、记忆与画像、模块与提示、调试与降级、调用流程、调试建议。
- 如果侧边栏未显示，请确认 `index.html` 中启用了 `loadSidebar: true`，并且仓库根目录存在 `_sidebar.md`。

> 提示：本压缩包仅包含 **新增文档文件** 与 `_sidebar.md`，不会更改你的 `index.html` 或 CSS。
